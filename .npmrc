# react-sortable-hoc@2 (used by the profile reorder UI) declares a react ^16 || ^17 peer,
# while the @wordpress/* packages require react 18. npm 7+ treats that as a hard install
# failure, so plain `npm install` errors out of the box without this.
legacy-peer-deps=true
